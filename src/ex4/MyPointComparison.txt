ア
　MyPointのインスタンスa, b, c, dを作成している

イ
　変数dに対して変数aの参照先の値を代入している

ウ
　equals()メソッドはオブジェクトが同じかどうかを判定するメソッドである。変数a, bの値は違うのでfalseとなる

エ
　変数a, cの値はどちらとも(6,8)なのでtrueとなる

オ
　変数dは、変数aの参照値のコピーである。つまり、どちらの変数も値は(6,8)なのでtrueとなる

カ
　a == bは変数aとbの参照先を比較している。

キ
　変数aとbは参照先が異なるオブジェクトなので、falseとなる

ク
　変数aとcは値は同じでも、参照先が異なるので、falseとなる

ケ
　変数aとdは、d=a;として参照先の値を代入したように、両者は参照先が同じものなのでtrueとなる
10. はなぜこのような結果(true)になるのか説明しなさい。